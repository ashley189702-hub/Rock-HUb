do print("Rock Hub Loaded✅");local Players=game:GetService("Players");local RunService=game:GetService("RunService");local TweenService=game:GetService("TweenService");local HttpService=game:GetService("HttpService");local UserInputService=game:GetService("UserInputService");local StarterGui=game:GetService("StarterGui");local Workspace=game:GetService("Workspace");local Stats=game:GetService("Stats");local ProximityPromptService=game:GetService("ProximityPromptService");local LocalPlayer=Players.LocalPlayer;local player=LocalPlayer;local logoAssetId="rbxassetid://94555150411788";local uiClosed=false;local heartbeatConnection=nil;local physicsConnection=nil;local inputConnection=nil;local currentWaiting=nil;local DEFAULT={ESP=false,ESPColor=Color3.fromRGB(180,70,255),Gravity=false,Unwalk=false,AntiKnockback=false,Speed55=false,Spin=false,SpeedText="50",SpeedActive=false,SpeedPopVisible=false,SpeedMinimized=false,XRay=false,Optimizer=false,Float=false,AutoBat=false,AutoSteal=false,StealStatusMinimized=false,FPSPingPopVisible=false,FPSPingMinimized=false,FloatPopVisible=false,FloatPopMinimized=false,HitboxExpander=false,GUIKey=nil,ESPKey=nil,XRayKey=nil,GravityKey=nil,UnwalkKey=nil,AntiKnockbackKey=nil,Speed55Key=nil,SpeedBoosterKey=nil,OptimizerKey=nil,SpinKey=nil,FloatKey=nil,AutoBatKey=nil,AutoStealKey=nil,HitboxExpanderKey=nil,StealRadius=20,StealDuration=1.3};local settings={};for k,v in pairs(DEFAULT) do settings[k]=v;end local configFile="RockHubConfig.json";local FLOAT_HEIGHT=20;local targetFloatY=nil;local floatEnabled=settings.Float;local floatPart=nil;local floatBP=nil;local floatCountdownThread=nil;local floatCountdownTime=20;local autoBatEnabled=settings.AutoBat;local currentBatTool=nil;local lastBatHit=0;local BAT_HIT_DELAY=0.25;local BAT_RANGE=12;local batRadiusPart=nil;local batRadiusHighlight=nil;local autoStealEnabled=settings.AutoSteal;local STEAL_RADIUS=settings.StealRadius or 20 ;local STEAL_DURATION=settings.StealDuration or 1.3 ;local isStealing=false;local StealData={};local autoStealConnection=nil;local stealStatusPop=nil;local stealStatusText=nil;local stealStatusTitle=nil;local stealStatusStroke=nil;local progressBarFill=nil;local function isMyPlotByName(plotName) local plots=workspace:FindFirstChild("Plots");if  not plots then return false;end local plot=plots:FindFirstChild(plotName);if  not plot then return false;end local sign=plot:FindFirstChild("PlotSign");if sign then local yourBase=sign:FindFirstChild("YourBase");if (yourBase and yourBase:IsA("BillboardGui")) then return yourBase.Enabled==true ;end end return false;end local function findNearestPrompt() local h=LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") ;if  not h then return nil;end local plots=workspace:FindFirstChild("Plots");if  not plots then return nil;end local np,nd=nil,math.huge;for _,plot in ipairs(plots:GetChildren()) do if isMyPlotByName(plot.Name) then continue;end local podiums=plot:FindFirstChild("AnimalPodiums");if  not podiums then continue;end for _,pod in ipairs(podiums:GetChildren()) do pcall(function() local base=pod:FindFirstChild("Base");local spawn=base and base:FindFirstChild("Spawn") ;if spawn then local dist=(spawn.Position-h.Position).Magnitude;if ((dist<nd) and (dist<=STEAL_RADIUS)) then local att=spawn:FindFirstChild("PromptAttachment");if att then for _,ch in ipairs(att:GetChildren()) do if ch:IsA("ProximityPrompt") then np,nd=ch,dist;break;end end end end end end);end end return np;end local function updateStealStatus(status,progress) if ( not stealStatusPop or  not stealStatusText) then return;end if (status=="stealing") then stealStatusText.Text="STEALING...";stealStatusText.TextColor3=Color3.fromRGB(255,100,100);elseif (status=="searching") then stealStatusText.Text="SEARCHING...";stealStatusText.TextColor3=Color3.fromRGB(255,200,100);elseif (status=="ready") then stealStatusText.Text="READY";stealStatusText.TextColor3=Color3.fromRGB(100,255,100);elseif (status=="off") then stealStatusText.Text="OFF";stealStatusText.TextColor3=Color3.fromRGB(200,200,255);end if (progressBarFill and progress) then progressBarFill.Size=UDim2.new(math.clamp(progress,0,1),0,1,0);elseif progressBarFill then progressBarFill.Size=UDim2.new(0,0,1,0);end end local function executeSteal(prompt) if isStealing then return;end if  not StealData[prompt] then StealData[prompt]={hold={},trigger={},ready=true};pcall(function() if getconnections then for _,c in ipairs(getconnections(prompt.PromptButtonHoldBegan)) do if c.Function then table.insert(StealData[prompt].hold,c.Function);end end for _,c in ipairs(getconnections(prompt.Triggered)) do if c.Function then table.insert(StealData[prompt].trigger,c.Function);end end end end);end local data=StealData[prompt];if  not data.ready then return;end data.ready=false;isStealing=true;updateStealStatus("stealing",0);local stealStartTime=tick();task.spawn(function() for _,f in ipairs(data.hold) do task.spawn(f);end local progressConnection=RunService.Heartbeat:Connect(function() if  not isStealing then return;end local prog=math.clamp((tick() -stealStartTime)/STEAL_DURATION ,0,1);updateStealStatus("stealing",prog);end);task.wait(STEAL_DURATION);progressConnection:Disconnect();for _,f in ipairs(data.trigger) do task.spawn(f);end data.ready=true;isStealing=false;updateStealStatus("ready",0);end);end function startAutoSteal() if autoStealConnection then return;end if stealStatusPop then stealStatusPop.Visible=true;updateStealStatus("searching",0);end autoStealConnection=RunService.Heartbeat:Connect(function() if  not isStealing then local p=findNearestPrompt();if p then executeSteal(p);else updateStealStatus("searching",0);end end end);end function stopAutoSteal() if autoStealConnection then autoStealConnection:Disconnect();autoStealConnection=nil;end isStealing=false;updateStealStatus("off",0);task.delay(2,function() if ( not autoStealEnabled and stealStatusPop) then stealStatusPop.Visible=false;end end);end local hitboxExpanderEnabled=settings.HitboxExpander;local hitboxObjects={};local HITBOX_EXPAND_SIZE=Vector3.new(13,13,13);local antiKnockbackEnabled=false;local antiConnections={};local BAD_STATES={[Enum.HumanoidStateType.Physics]=true,[Enum.HumanoidStateType.Ragdoll]=true,[Enum.HumanoidStateType.FallingDown]=true,[Enum.HumanoidStateType.GettingUp]=true};local PURPLE_DARK=Color3.fromRGB(20,10,35);local PURPLE_MED=Color3.fromRGB(55,30,90);local PURPLE_BRIGHT=Color3.fromRGB(170,60,240);local PURPLE_GLOW=Color3.fromRGB(200,100,255);local TEXT_COLOR=Color3.fromRGB(255,255,255);local TEXT_DIM=Color3.fromRGB(200,200,255);local ON_COLOR=PURPLE_BRIGHT;local OFF_COLOR=PURPLE_MED;local function ReEnableControls() pcall(function() require(LocalPlayer:WaitForChild("PlayerScripts"):WaitForChild("PlayerModule")):GetControls():Enable();end);end local function SetupCharacter(char) local humanoid=char:WaitForChild("Humanoid");local hrp=char:WaitForChild("HumanoidRootPart");local animator=humanoid:WaitForChild("Animator");local lastVelocity=Vector3.zero;local function Clean() for _,v in ipairs(char:GetDescendants()) do if (v:IsA("BallSocketConstraint") or v:IsA("NoCollisionConstraint") or v:IsA("HingeConstraint") or (v:IsA("Attachment") and ((v.Name=="A") or (v.Name=="B"))) or v:IsA("BodyVelocity") or v:IsA("BodyGyro") or v:IsA("BodyPosition") or v:IsA("BodyAngularVelocity")) then v:Destroy();elseif v:IsA("Motor6D") then v.Enabled=true;end end for _,track in ipairs(animator:GetPlayingAnimationTracks()) do local name=((track.Animation and track.Animation.Name) or ""):lower();if (name:find("rag") or name:find("fall") or name:find("hurt")) then track:Stop(0);end end end table.insert(antiConnections,humanoid.StateChanged:Connect(function(_,new) if BAD_STATES[new] then humanoid:ChangeState(Enum.HumanoidStateType.Running);ReEnableControls();Clean();end end));table.insert(antiConnections,RunService.Heartbeat:Connect(function() local vel=hrp.AssemblyLinearVelocity;local spike=(vel-lastVelocity).Magnitude;if ((spike>45) and (vel.Magnitude>30)) then local newX=0;local newZ=0;local newY=((vel.Y>80) and 0) or vel.Y ;hrp.AssemblyLinearVelocity=Vector3.new(newX,newY,newZ);end lastVelocity=vel;end));ReEnableControls();Clean();end local function EnableAntiKB() if antiKnockbackEnabled then return;end antiKnockbackEnabled=true;if LocalPlayer.Character then SetupCharacter(LocalPlayer.Character);end table.insert(antiConnections,LocalPlayer.CharacterAdded:Connect(function(char) task.wait(0.25);SetupCharacter(char);end));end local function DisableAntiKB() antiKnockbackEnabled=false;for _,c in antiConnections do c:Disconnect();end antiConnections={};end local function startFloatCountdown() if floatCountdownThread then task.cancel(floatCountdownThread);floatCountdownThread=nil;end floatCountdownThread=task.spawn(function() local timeLeft=floatCountdownTime;while (timeLeft>0) and floatEnabled  do if (floatPop and floatPopText) then floatPop.Visible=true;floatPopText.Text="Disappearing in "   .. timeLeft   .. "s" ;floatPopText.TextColor3=((timeLeft<=5) and Color3.fromRGB(255,50,50)) or Color3.fromRGB(255,200,50) ;floatPopTitle.Text="⚠️ FLOAT DISAPPEARING";floatPopTitle.TextColor3=Color3.fromRGB(255,50,50);if floatPopStroke then floatPopStroke.Color=((timeLeft<=5) and Color3.fromRGB(255,0,0)) or Color3.fromRGB(255,150,0) ;end end task.wait(1);timeLeft=timeLeft-1 ;end if (floatEnabled and (timeLeft<=0)) then floatEnabled=false;settings.Float=false;if floatBtn then floatBtn.Text="Float: OFF";floatBtn.BackgroundColor3=OFF_COLOR;floatBtn.BackgroundTransparency=0.3;end if floatBP then floatBP:Destroy();floatBP=nil;end if floatPart then floatPart:Destroy();floatPart=nil;end targetFloatY=nil;if (floatPop and floatPopText) then floatPopText.Text="Float Disabled!";floatPopText.TextColor3=Color3.fromRGB(255,50,50);floatPopTitle.Text="❌ FLOAT OFF";floatPopTitle.TextColor3=Color3.fromRGB(255,50,50);if floatPopStroke then floatPopStroke.Color=Color3.fromRGB(255,0,0);end end task.delay(2,function() if ( not floatEnabled and floatPop) then floatPop.Visible=false;end end);saveSettings(true);end floatCountdownThread=nil;end);end local function stopFloatCountdown() if floatCountdownThread then task.cancel(floatCountdownThread);floatCountdownThread=nil;end if floatPop then floatPop.Visible=false;end end local function showFloatActive() if  not floatPop then return;end floatPop.Visible=true;floatPopText.Text="Float Active - 20s";floatPopText.TextColor3=Color3.fromRGB(0,255,100);floatPopTitle.Text="✅ FLOAT ACTIVE";floatPopTitle.TextColor3=Color3.fromRGB(0,255,100);if floatPopStroke then floatPopStroke.Color=Color3.fromRGB(0,255,100);end end local function expandHitbox(character) local hrp=character:FindFirstChild("HumanoidRootPart");if (hrp and  not hitboxObjects[hrp]) then hitboxObjects[hrp]={Size=hrp.Size,Transparency=hrp.Transparency,CanCollide=hrp.CanCollide,Material=hrp.Material,Color=hrp.Color};hrp.Size=HITBOX_EXPAND_SIZE;hrp.Transparency=0.7;hrp.Material=Enum.Material.Neon;hrp.Color=PURPLE_BRIGHT;hrp.CanCollide=false;end end local function restoreHitbox(character) local hrp=character:FindFirstChild("HumanoidRootPart");if (hrp and hitboxObjects[hrp]) then hrp.Size=hitboxObjects[hrp].Size;hrp.Transparency=hitboxObjects[hrp].Transparency;hrp.Material=hitboxObjects[hrp].Material;hrp.Color=hitboxObjects[hrp].Color;hrp.CanCollide=hitboxObjects[hrp].CanCollide;hitboxObjects[hrp]=nil;end end local function enableHitboxExpander() for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character) then expandHitbox(plr.Character);end end Players.PlayerAdded:Connect(function(plr) if (hitboxExpanderEnabled and (plr~=LocalPlayer)) then plr.CharacterAdded:Connect(function(char) task.wait(0.5);expandHitbox(char);end);if plr.Character then expandHitbox(plr.Character);end end end);end local function disableHitboxExpander() for hrp,_ in pairs(hitboxObjects) do if (hrp and hrp.Parent) then restoreHitbox(hrp.Parent);end end hitboxObjects={};end local function loadSettings() local success,err=pcall(function() if  not isfile(configFile) then return false;end local data=readfile(configFile);local loaded=HttpService:JSONDecode(data);for k,v in pairs(DEFAULT) do if (loaded[k]~=nil) then if ((k=="ESPColor") and (typeof(loaded[k])=="table")) then settings[k]=Color3.new(loaded[k].r,loaded[k].g,loaded[k].b);elseif ((typeof(v)=="EnumItem") or (v==nil)) then if ((loaded[k]=="None") or (loaded[k]==nil)) then settings[k]=nil;else local keySuccess,keyEnum=pcall(Enum.KeyCode[loaded[k]]);settings[k]=(keySuccess and keyEnum) or nil ;end else settings[k]=loaded[k];end end end if loaded.StealRadius then STEAL_RADIUS=loaded.StealRadius;end if loaded.StealDuration then STEAL_DURATION=loaded.StealDuration;end return true;end);if  not success then warn("RockHub load error:",err);end return success;end local function saveSettings(silent) local success,err=pcall(function() local toSave={};for k,v in pairs(settings) do if (k=="ESPColor") then toSave[k]={r=v.R,g=v.G,b=v.B};elseif (v==nil) then toSave[k]="None";elseif (typeof(v)=="EnumItem") then toSave[k]=tostring(v);else toSave[k]=v;end end toSave.StealRadius=STEAL_RADIUS;toSave.StealDuration=STEAL_DURATION;writefile(configFile,HttpService:JSONEncode(toSave));end);if (success and  not silent) then StarterGui:SetCore("SendNotification",{Title="RockHub",Text="Config saved!",Duration=1.5});elseif  not success then warn("RockHub save error:",err);end return success;end local configLoaded=false;if isfile(configFile) then configLoaded=loadSettings();end local screenGui=Instance.new("ScreenGui");screenGui.Name="RockHub";screenGui.ResetOnSpawn=false;screenGui.Parent=player:WaitForChild("PlayerGui");local mainFrame=Instance.new("Frame");mainFrame.Size=UDim2.new(0,480,0,420);mainFrame.Position=UDim2.new(0.5, -240,0.5, -210);mainFrame.BackgroundColor3=PURPLE_DARK;mainFrame.BackgroundTransparency=0.15;mainFrame.BorderSizePixel=0;mainFrame.ClipsDescendants=true;mainFrame.Parent=screenGui;Instance.new("UICorner",mainFrame).CornerRadius=UDim.new(0,16);local mainStroke=Instance.new("UIStroke");mainStroke.Color=PURPLE_GLOW;mainStroke.Thickness=3;mainStroke.Transparency=0.4;mainStroke.Parent=mainFrame;local titleBar=Instance.new("Frame");titleBar.Size=UDim2.new(1,0,0,50);titleBar.BackgroundTransparency=1;titleBar.Parent=mainFrame;local logo=Instance.new("ImageLabel");logo.Size=UDim2.new(0,40,0,40);logo.Position=UDim2.new(0,12,0,5);logo.BackgroundTransparency=1;logo.Image=logoAssetId;logo.ScaleType=Enum.ScaleType.Fit;logo.Parent=titleBar;Instance.new("UICorner",logo).CornerRadius=UDim.new(0,10);local title=Instance.new("TextLabel");title.Size=UDim2.new(1, -160,1,0);title.Position=UDim2.new(0,60,0,0);title.BackgroundTransparency=1;title.Text="ROCK HUB";title.TextColor3=PURPLE_BRIGHT;title.Font=Enum.Font.GothamBlack;title.TextSize=24;title.TextXAlignment=Enum.TextXAlignment.Left;title.Parent=titleBar;local closeBtn=Instance.new("TextButton");closeBtn.Size=UDim2.new(0,40,0,40);closeBtn.Position=UDim2.new(1, -50,0,5);closeBtn.BackgroundColor3=Color3.fromRGB(200,40,60);closeBtn.BackgroundTransparency=0.3;closeBtn.Text="X";closeBtn.TextColor3=Color3.fromRGB(255,255,255);closeBtn.Font=Enum.Font.GothamBold;closeBtn.TextSize=26;closeBtn.Parent=titleBar;Instance.new("UICorner",closeBtn).CornerRadius=UDim.new(0,10);local minimizeBtn=Instance.new("TextButton");minimizeBtn.Size=UDim2.new(0,40,0,40);minimizeBtn.Position=UDim2.new(1, -95,0,5);minimizeBtn.BackgroundColor3=PURPLE_MED;minimizeBtn.BackgroundTransparency=0.3;minimizeBtn.Text="−";minimizeBtn.TextColor3=Color3.fromRGB(255,255,255);minimizeBtn.Font=Enum.Font.GothamBold;minimizeBtn.TextSize=32;minimizeBtn.Parent=titleBar;Instance.new("UICorner",minimizeBtn).CornerRadius=UDim.new(0,10);local function makeDraggable(frame) local dragging,dragStart,startPos;frame.InputBegan:Connect(function(i) if (i.UserInputType==Enum.UserInputType.MouseButton1) then dragging=true;dragStart=i.Position;startPos=frame.Position;i.Changed:Connect(function() if (i.UserInputState==Enum.UserInputState.End) then dragging=false;end end);end end);frame.InputChanged:Connect(function(i) if (dragging and (i.UserInputType==Enum.UserInputType.MouseMovement)) then local delta=i.Position-dragStart ;frame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset + delta.X ,startPos.Y.Scale,startPos.Y.Offset + delta.Y );end end);end makeDraggable(mainFrame);local minimized=false;minimizeBtn.MouseButton1Click:Connect(function() minimized= not minimized;local targetSize=(minimized and UDim2.new(0,480,0,50)) or UDim2.new(0,480,0,420) ;TweenService:Create(mainFrame,TweenInfo.new(0.35,Enum.EasingStyle.Quad),{Size=targetSize}):Play();tabPanel.Visible= not minimized;contentArea.Visible= not minimized;end);local tabPanel=Instance.new("Frame");tabPanel.Size=UDim2.new(0,140,1, -60);tabPanel.Position=UDim2.new(0,10,0,50);tabPanel.BackgroundColor3=PURPLE_MED;tabPanel.BackgroundTransparency=0.4;tabPanel.Parent=mainFrame;Instance.new("UICorner",tabPanel).CornerRadius=UDim.new(0,14);local tabLayout=Instance.new("UIListLayout");tabLayout.Padding=UDim.new(0,12);tabLayout.HorizontalAlignment=Enum.HorizontalAlignment.Center;tabLayout.Parent=tabPanel;local tabPad=Instance.new("UIPadding");tabPad.PaddingTop=UDim.new(0,8);tabPad.Parent=tabPanel;local contentArea=Instance.new("Frame");contentArea.Size=UDim2.new(1, -160,1, -70);contentArea.Position=UDim2.new(0,150,0,55);contentArea.BackgroundTransparency=1;contentArea.Parent=mainFrame;local activeTabBtn=nil;local function createTab(text) local btn=Instance.new("TextButton");btn.Size=UDim2.new(1, -16,0,50);btn.BackgroundColor3=PURPLE_MED;btn.BackgroundTransparency=0.3;btn.Text=" "   .. text ;btn.TextColor3=TEXT_DIM;btn.Font=Enum.Font.GothamBold;btn.TextSize=18;btn.TextXAlignment=Enum.TextXAlignment.Left;btn.AutoButtonColor=false;btn.Parent=tabPanel;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,12);local stroke=Instance.new("UIStroke");stroke.Color=PURPLE_GLOW;stroke.Thickness=2;stroke.Transparency=0.7;stroke.Parent=btn;return btn,stroke;end local btnVisuals,strokeVisuals=createTab("Visuals");local btnMove,strokeMove=createTab("Main");local btnMisc,strokeMisc=createTab("Misc");local btnKeybinds,strokeKeybinds=createTab("Keybinds");local pageVisuals=Instance.new("ScrollingFrame");pageVisuals.Size=UDim2.new(1,0,1,0);pageVisuals.BackgroundTransparency=1;pageVisuals.ScrollBarThickness=5;pageVisuals.ScrollBarImageColor3=PURPLE_GLOW;pageVisuals.AutomaticCanvasSize=Enum.AutomaticSize.Y;pageVisuals.Parent=contentArea;local pageMove=Instance.new("ScrollingFrame");pageMove.Size=UDim2.new(1,0,1,0);pageMove.BackgroundTransparency=1;pageMove.ScrollBarThickness=5;pageMove.ScrollBarImageColor3=PURPLE_GLOW;pageMove.AutomaticCanvasSize=Enum.AutomaticSize.Y;pageMove.Visible=false;pageMove.Parent=contentArea;local pageMisc=Instance.new("ScrollingFrame");pageMisc.Size=UDim2.new(1,0,1,0);pageMisc.BackgroundTransparency=1;pageMisc.ScrollBarThickness=5;pageMisc.ScrollBarImageColor3=PURPLE_GLOW;pageMisc.AutomaticCanvasSize=Enum.AutomaticSize.Y;pageMisc.Visible=false;pageMisc.Parent=contentArea;local pageKeybinds=Instance.new("ScrollingFrame");pageKeybinds.Size=UDim2.new(1,0,1,0);pageKeybinds.BackgroundTransparency=1;pageKeybinds.ScrollBarThickness=5;pageKeybinds.ScrollBarImageColor3=PURPLE_GLOW;pageKeybinds.AutomaticCanvasSize=Enum.AutomaticSize.Y;pageKeybinds.Visible=false;pageKeybinds.Parent=contentArea;local function switchTab(tabBtn,stroke,page) if activeTabBtn then TweenService:Create(activeTabBtn,TweenInfo.new(0.25),{BackgroundTransparency=0.3,TextColor3=TEXT_DIM}):Play();TweenService:Create(activeTabBtn:FindFirstChild("UIStroke"),TweenInfo.new(0.25),{Transparency=0.7}):Play();end TweenService:Create(tabBtn,TweenInfo.new(0.25),{BackgroundTransparency=0,TextColor3=TEXT_COLOR}):Play();TweenService:Create(stroke,TweenInfo.new(0.25),{Transparency=0}):Play();pageVisuals.Visible=false;pageMove.Visible=false;pageMisc.Visible=false;pageKeybinds.Visible=false;page.Visible=true;activeTabBtn=tabBtn;end btnVisuals.MouseButton1Click:Connect(function() switchTab(btnVisuals,strokeVisuals,pageVisuals);end);btnMove.MouseButton1Click:Connect(function() switchTab(btnMove,strokeMove,pageMove);end);btnMisc.MouseButton1Click:Connect(function() switchTab(btnMisc,strokeMisc,pageMisc);end);btnKeybinds.MouseButton1Click:Connect(function() switchTab(btnKeybinds,strokeKeybinds,pageKeybinds);end);switchTab(btnVisuals,strokeVisuals,pageVisuals);local function addPageLayout(page) local layout=Instance.new("UIListLayout");layout.Padding=UDim.new(0,12);layout.HorizontalAlignment=Enum.HorizontalAlignment.Center;layout.Parent=page;local pad=Instance.new("UIPadding");pad.PaddingTop=UDim.new(0,15);pad.PaddingLeft=UDim.new(0,8);pad.PaddingRight=UDim.new(0,8);pad.PaddingBottom=UDim.new(0,15);pad.Parent=page;end addPageLayout(pageVisuals);addPageLayout(pageMove);addPageLayout(pageMisc);addPageLayout(pageKeybinds);local function createToggleButton(parent,text,initialState) local btn=Instance.new("TextButton");btn.Size=UDim2.new(1, -16,0,55);btn.BackgroundColor3=(initialState and ON_COLOR) or OFF_COLOR ;btn.BackgroundTransparency=(initialState and 0.1) or 0.3 ;btn.Text=text;btn.TextColor3=TEXT_COLOR;btn.Font=Enum.Font.GothamBold;btn.TextSize=20;btn.Parent=parent;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,16);local stroke=Instance.new("UIStroke");stroke.Color=PURPLE_GLOW;stroke.Thickness=2.5;stroke.Transparency=0.4;stroke.Parent=btn;return btn;end local espEnabled=settings.ESP;local espColor=settings.ESPColor;local espObjects={};local espBtn=createToggleButton(pageVisuals,(espEnabled and "ESP: ON") or "ESP: OFF" ,espEnabled);espBtn.MouseButton1Click:Connect(function() espEnabled= not espEnabled;settings.ESP=espEnabled;espBtn.Text=(espEnabled and "ESP: ON") or "ESP: OFF" ;espBtn.BackgroundColor3=(espEnabled and ON_COLOR) or OFF_COLOR ;espBtn.BackgroundTransparency=(espEnabled and 0.1) or 0.3 ;saveSettings(true);end);local colorFrame=Instance.new("Frame");colorFrame.Size=UDim2.new(1, -16,0,45);colorFrame.BackgroundTransparency=1;colorFrame.Parent=pageVisuals;local colorLayout=Instance.new("UIListLayout");colorLayout.FillDirection=Enum.FillDirection.Horizontal;colorLayout.Padding=UDim.new(0,15);colorLayout.HorizontalAlignment=Enum.HorizontalAlignment.Center;colorLayout.Parent=colorFrame;local colors={{"Purple",Color3.fromRGB(180,70,255)},{"Red",Color3.fromRGB(255,0,0)},{"Green",Color3.fromRGB(0,255,0)}};for _,data in ipairs(colors) do local btn=Instance.new("TextButton");btn.Size=UDim2.new(0,100,1,0);btn.BackgroundColor3=data[2];btn.BackgroundTransparency=0.2;btn.Text=data[1];btn.TextColor3=Color3.fromRGB(255,255,255);btn.Font=Enum.Font.GothamBold;btn.TextSize=17;btn.Parent=colorFrame;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,12);local stroke=Instance.new("UIStroke");stroke.Color=PURPLE_GLOW;stroke.Thickness=2;stroke.Parent=btn;btn.MouseButton1Click:Connect(function() espColor=data[2];settings.ESPColor=data[2];saveSettings(true);end);end local xrayEnabled=settings.XRay;local xrayBtn=createToggleButton(pageVisuals,(xrayEnabled and "X-Ray: ON") or "X-Ray: OFF" ,xrayEnabled);local xrayObjects={};local excludedNames={"BasePlate","Baseplate","Terrain","Ground","Floor","Grass","Soil"};local xrayTransparency=0.5;local function applyXRay(obj) if (obj:IsA("BasePart") and (obj.Transparency<1)) then for _,name in ipairs(excludedNames) do if obj.Name:lower():find(name:lower()) then return;end end local char=LocalPlayer.Character;if (char and obj:IsDescendantOf(char)) then return;end if  not xrayObjects[obj] then xrayObjects[obj]=obj.Transparency;end obj.Transparency=xrayTransparency;obj.LocalTransparencyModifier=xrayTransparency;end end local function clearXRay() for obj,orig in pairs(xrayObjects) do if (obj and obj.Parent) then obj.Transparency=orig or 0 ;obj.LocalTransparencyModifier=0;end end xrayObjects={};end local function enableXRay() for _,obj in pairs(workspace:GetDescendants()) do applyXRay(obj);end workspace.DescendantAdded:Connect(function(obj) if xrayEnabled then applyXRay(obj);end end);end xrayBtn.MouseButton1Click:Connect(function() xrayEnabled= not xrayEnabled;settings.XRay=xrayEnabled;xrayBtn.Text=(xrayEnabled and "X-Ray: ON") or "X-Ray: OFF" ;xrayBtn.BackgroundColor3=(xrayEnabled and ON_COLOR) or OFF_COLOR ;xrayBtn.BackgroundTransparency=(xrayEnabled and 0.1) or 0.3 ;if xrayEnabled then enableXRay();else clearXRay();end saveSettings(true);end);local hitboxExpanderBtn=createToggleButton(pageVisuals,(hitboxExpanderEnabled and "Hitbox Expander: ON") or "Hitbox Expander: OFF" ,hitboxExpanderEnabled);hitboxExpanderBtn.MouseButton1Click:Connect(function() hitboxExpanderEnabled= not hitboxExpanderEnabled;settings.HitboxExpander=hitboxExpanderEnabled;hitboxExpanderBtn.Text=(hitboxExpanderEnabled and "Hitbox Expander: ON") or "Hitbox Expander: OFF" ;hitboxExpanderBtn.BackgroundColor3=(hitboxExpanderEnabled and ON_COLOR) or OFF_COLOR ;hitboxExpanderBtn.BackgroundTransparency=(hitboxExpanderEnabled and 0.1) or 0.3 ;if hitboxExpanderEnabled then enableHitboxExpander();else disableHitboxExpander();end saveSettings(true);end);local gravityBtn=createToggleButton(pageMove,(settings.Gravity and "Low Gravity: ON") or "Low Gravity: OFF" ,settings.Gravity);local gravityOn=settings.Gravity;local force=nil;gravityBtn.MouseButton1Click:Connect(function() gravityOn= not gravityOn;settings.Gravity=gravityOn;gravityBtn.Text=(gravityOn and "Low Gravity: ON") or "Low Gravity: OFF" ;gravityBtn.BackgroundColor3=(gravityOn and ON_COLOR) or OFF_COLOR ;gravityBtn.BackgroundTransparency=(gravityOn and 0.1) or 0.3 ;local char=player.Character;local hrp=char and char:FindFirstChild("HumanoidRootPart") ;if (gravityOn and hrp) then if force then force:Destroy();end force=Instance.new("BodyForce");force.Force=Vector3.new(0,workspace.Gravity * hrp:GetMass() * 0.7 ,0);force.Parent=hrp;elseif force then force:Destroy();force=nil;end saveSettings(true);end);LocalPlayer.CharacterAdded:Connect(function(char) task.wait(0.1);if gravityOn then local hrp=char:FindFirstChild("HumanoidRootPart");if hrp then if force then force:Destroy();end force=Instance.new("BodyForce");force.Force=Vector3.new(0,workspace.Gravity * hrp:GetMass() * 0.7 ,0);force.Parent=hrp;end end end);local unwalkBtn=createToggleButton(pageMove,(settings.Unwalk and "No Walk Anim: ON") or "No Walk Anim: OFF" ,settings.Unwalk);local unwalk=settings.Unwalk;local function updateUnwalk() RunService:UnbindFromRenderStep("StopAnims");if unwalk then RunService:BindToRenderStep("StopAnims",301,function() if uiClosed then return;end local hum=player.Character and player.Character:FindFirstChildOfClass("Humanoid") ;if hum then for _,track in ipairs(hum:GetPlayingAnimationTracks()) do track:Stop();end end end);end end updateUnwalk();unwalkBtn.MouseButton1Click:Connect(function() unwalk= not unwalk;settings.Unwalk=unwalk;unwalkBtn.Text=(unwalk and "No Walk Anim: ON") or "No Walk Anim: OFF" ;unwalkBtn.BackgroundColor3=(unwalk and ON_COLOR) or OFF_COLOR ;unwalkBtn.BackgroundTransparency=(unwalk and 0.1) or 0.3 ;updateUnwalk();saveSettings(true);end);local antiKBBtn=createToggleButton(pageMove,(settings.AntiKnockback and "Anti Knockback: ON") or "Anti Knockback: OFF" ,settings.AntiKnockback);antiKBBtn.MouseButton1Click:Connect(function() settings.AntiKnockback= not settings.AntiKnockback;antiKBBtn.Text=(settings.AntiKnockback and "Anti Knockback: ON") or "Anti Knockback: OFF" ;antiKBBtn.BackgroundColor3=(settings.AntiKnockback and ON_COLOR) or OFF_COLOR ;antiKBBtn.BackgroundTransparency=(settings.AntiKnockback and 0.1) or 0.3 ;if settings.AntiKnockback then EnableAntiKB();else DisableAntiKB();end saveSettings(true);end);local speed55Btn=createToggleButton(pageMove,(settings.Speed55 and "Speed 55: ON") or "Speed 55: OFF" ,settings.Speed55);local speed55Active=settings.Speed55;speed55Btn.MouseButton1Click:Connect(function() speed55Active= not speed55Active;settings.Speed55=speed55Active;speed55Btn.Text=(speed55Active and "Speed 55: ON") or "Speed 55: OFF" ;speed55Btn.BackgroundColor3=(speed55Active and ON_COLOR) or OFF_COLOR ;speed55Btn.BackgroundTransparency=(speed55Active and 0.1) or 0.3 ;saveSettings(true);end);local spinBtn=createToggleButton(pageMove,(settings.Spin and "Spin: ON") or "Spin: OFF" ,settings.Spin);local spinEnabled=settings.Spin;local SPIN_SPEED=35;local spinBAV=nil;local function applySpin(char) local hrp=char:FindFirstChild("HumanoidRootPart");if  not hrp then return;end if (spinBAV and spinBAV.Parent) then spinBAV:Destroy();end spinBAV=Instance.new("BodyAngularVelocity");spinBAV.Name="RockHubSpin";spinBAV.AngularVelocity=Vector3.new(0,SPIN_SPEED,0);spinBAV.MaxTorque=Vector3.new(0,math.huge,0);spinBAV.P=1250;spinBAV.Parent=hrp;end local function removeSpin() if (spinBAV and spinBAV.Parent) then spinBAV:Destroy();spinBAV=nil;end end spinBtn.MouseButton1Click:Connect(function() spinEnabled= not spinEnabled;settings.Spin=spinEnabled;spinBtn.Text=(spinEnabled and "Spin: ON") or "Spin: OFF" ;spinBtn.BackgroundColor3=(spinEnabled and ON_COLOR) or OFF_COLOR ;spinBtn.BackgroundTransparency=(spinEnabled and 0.1) or 0.3 ;if (spinEnabled and player.Character) then applySpin(player.Character);else removeSpin();end saveSettings(true);end);LocalPlayer.CharacterAdded:Connect(function(char) task.wait(0.1);if spinEnabled then applySpin(char);end end);local function createFloatPlatform() if floatPart then floatPart:Destroy();end floatPart=Instance.new("Part");floatPart.Name="RockHubFloatPlatform";floatPart.Size=Vector3.new(4,0.8,4);floatPart.Color=PURPLE_BRIGHT;floatPart.Material=Enum.Material.Neon;floatPart.Anchored=true;floatPart.CanCollide=true;floatPart.Transparency=0.3;floatPart.Parent=workspace;end local function setupFloatBP(hrp) if (floatBP and floatBP.Parent) then floatBP:Destroy();end floatBP=Instance.new("BodyPosition");floatBP.Name="RockHubFloatBP";floatBP.MaxForce=Vector3.new(0,math.huge,0);floatBP.P=8000;floatBP.D=1200;floatBP.Parent=hrp;end local floatBtn=createToggleButton(pageMove,(floatEnabled and "Float: ON") or "Float: OFF" ,floatEnabled);floatBtn.MouseButton1Click:Connect(function() floatEnabled= not floatEnabled;settings.Float=floatEnabled;floatBtn.Text=(floatEnabled and "Float: ON") or "Float: OFF" ;floatBtn.BackgroundColor3=(floatEnabled and ON_COLOR) or OFF_COLOR ;floatBtn.BackgroundTransparency=(floatEnabled and 0.1) or 0.3 ;local hrp=player.Character and player.Character:FindFirstChild("HumanoidRootPart") ;if (floatEnabled and hrp) then if  not targetFloatY then targetFloatY=hrp.Position.Y + FLOAT_HEIGHT ;end createFloatPlatform();setupFloatBP(hrp);startFloatCountdown();else if floatBP then floatBP:Destroy();floatBP=nil;end if floatPart then floatPart:Destroy();floatPart=nil;end targetFloatY=nil;stopFloatCountdown();end saveSettings(true);end);LocalPlayer.CharacterAdded:Connect(function(char) task.wait(0.5);if floatEnabled then local hrp=char:FindFirstChild("HumanoidRootPart");if hrp then createFloatPlatform();setupFloatBP(hrp);startFloatCountdown();end end end);local backpack=player:WaitForChild("Backpack");local function getCharacter() return player.Character or player.CharacterAdded:Wait() ;end local function equipBat() local char=getCharacter();local humanoid=char:FindFirstChildOfClass("Humanoid");if  not humanoid then return;end if (currentBatTool and (currentBatTool.Parent==char) and (currentBatTool.Name=="Bat")) then return;end for _,tool in ipairs(backpack:GetChildren()) do if (tool:IsA("Tool") and (tool.Name=="Bat")) then currentBatTool=tool;humanoid:EquipTool(tool);return;end end end local function isPlayerInRange(range) local char=LocalPlayer.Character;local hrp=char and char:FindFirstChild("HumanoidRootPart") ;if  not hrp then return false;end for _,plr in ipairs(Players:GetPlayers()) do if (plr~=LocalPlayer) then local otherChar=plr.Character;local otherHrp=otherChar and otherChar:FindFirstChild("HumanoidRootPart") ;if otherHrp then local distance=(hrp.Position-otherHrp.Position).Magnitude;if (distance<=range) then return true;end end end end return false;end local function createBatRadiusVisual() if batRadiusPart then return;end batRadiusPart=Instance.new("Part");batRadiusPart.Name="RockHubBatRadius";batRadiusPart.Shape=Enum.PartType.Cylinder;batRadiusPart.Size=Vector3.new(0.2,BAT_RANGE * 2 ,BAT_RANGE * 2 );batRadiusPart.Orientation=Vector3.new(0,0,90);batRadiusPart.Anchored=true;batRadiusPart.CanCollide=false;batRadiusPart.Transparency=0.7;batRadiusPart.Material=Enum.Material.Neon;batRadiusPart.Color=Color3.fromRGB(255,50,50);batRadiusPart.Parent=workspace.Terrain;local selectionBox=Instance.new("SelectionBox");selectionBox.Name="RadiusOutline";selectionBox.Adornee=batRadiusPart;selectionBox.Color3=Color3.fromRGB(255,255,255);selectionBox.LineThickness=0.05;selectionBox.Parent=batRadiusPart;end local function updateBatRadiusVisual() if  not autoBatEnabled then if batRadiusPart then batRadiusPart:Destroy();batRadiusPart=nil;end return;end local char=LocalPlayer.Character;local hrp=char and char:FindFirstChild("HumanoidRootPart") ;if  not hrp then if batRadiusPart then batRadiusPart:Destroy();batRadiusPart=nil;end return;end if  not batRadiusPart then createBatRadiusVisual();end if batRadiusPart then local groundY=(hrp.Position.Y-(hrp.Size.Y/2)) + 0.1 ;batRadiusPart.Position=Vector3.new(hrp.Position.X,groundY,hrp.Position.Z);local playerInRange=isPlayerInRange(BAT_RANGE);if playerInRange then batRadiusPart.Color=Color3.fromRGB(0,255,100);else batRadiusPart.Color=Color3.fromRGB(255,50,50);end end end local function cleanupBatRadius() if batRadiusPart then batRadiusPart:Destroy();batRadiusPart=nil;end end local function trackBatCharacter(char) char.ChildAdded:Connect(function(obj) if (obj:IsA("Tool") and (obj.Name=="Bat")) then currentBatTool=obj;elseif (obj:IsA("Tool") and autoBatEnabled) then autoBatEnabled=false;settings.AutoBat=false;autoBatBtn.Text="Auto Bat: OFF";autoBatBtn.BackgroundColor3=OFF_COLOR;autoBatBtn.BackgroundTransparency=0.3;cleanupBatRadius();currentBatTool=obj;end end);char.ChildRemoved:Connect(function(obj) if (obj==currentBatTool) then currentBatTool=nil;end end);end if player.Character then trackBatCharacter(player.Character);end player.CharacterAdded:Connect(function(char) task.wait(0.1);trackBatCharacter(char);end);local autoBatBtn=createToggleButton(pageMove,(autoBatEnabled and "Auto Bat: ON") or "Auto Bat: OFF" ,autoBatEnabled);autoBatBtn.MouseButton1Click:Connect(function() if (currentBatTool and (currentBatTool.Name~="Bat")) then equipBat();end autoBatEnabled= not autoBatEnabled;settings.AutoBat=autoBatEnabled;autoBatBtn.Text=(autoBatEnabled and "Auto Bat: ON") or "Auto Bat: OFF" ;autoBatBtn.BackgroundColor3=(autoBatEnabled and ON_COLOR) or OFF_COLOR ;autoBatBtn.BackgroundTransparency=(autoBatEnabled and 0.1) or 0.3 ;if autoBatEnabled then equipBat();createBatRadiusVisual();else cleanupBatRadius();end saveSettings(true);end);local function addPopoutStyle(frame) frame.BackgroundColor3=PURPLE_DARK;frame.BackgroundTransparency=0.2;Instance.new("UICorner",frame).CornerRadius=UDim.new(0,16);local glow=Instance.new("UIStroke");glow.Color=PURPLE_GLOW;glow.Thickness=3;glow.Transparency=0.3;glow.Parent=frame;return glow;end local function addMinimizeStyle(btn) btn.BackgroundColor3=PURPLE_MED;btn.BackgroundTransparency=0.3;btn.Text="−";btn.TextColor3=Color3.fromRGB(255,255,255);btn.Font=Enum.Font.GothamBold;btn.TextSize=26;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,10);local glow=Instance.new("UIStroke");glow.Color=PURPLE_GLOW;glow.Thickness=2;glow.Parent=btn;end local floatPop=Instance.new("Frame");floatPop.Size=UDim2.new(0,220,0,80);floatPop.Position=UDim2.new(0,600,0,360);local floatPopStroke=addPopoutStyle(floatPop);floatPop.Visible=false;floatPop.Parent=screenGui;makeDraggable(floatPop);local floatPopTitle=Instance.new("TextLabel");floatPopTitle.Size=UDim2.new(1,0,0,28);floatPopTitle.BackgroundTransparency=1;floatPopTitle.Text="❌ FLOAT OFF";floatPopTitle.TextColor3=Color3.fromRGB(255,50,50);floatPopTitle.Font=Enum.Font.GothamBlack;floatPopTitle.TextSize=16;floatPopTitle.Parent=floatPop;local floatPopText=Instance.new("TextLabel");floatPopText.Size=UDim2.new(1, -16,0,40);floatPopText.Position=UDim2.new(0,8,0,32);floatPopText.BackgroundTransparency=1;floatPopText.Text="Enable Float to start";floatPopText.TextColor3=Color3.fromRGB(200,200,255);floatPopText.Font=Enum.Font.GothamBold;floatPopText.TextSize=18;floatPopText.Parent=floatPop;local floatMinimizeBtn=Instance.new("TextButton");floatMinimizeBtn.Size=UDim2.new(0,28,0,28);floatMinimizeBtn.Position=UDim2.new(1, -32,0,4);floatMinimizeBtn.Parent=floatPop;addMinimizeStyle(floatMinimizeBtn);local floatPopMinimized=settings.FloatPopMinimized;floatMinimizeBtn.MouseButton1Click:Connect(function() floatPopMinimized= not floatPopMinimized;settings.FloatPopMinimized=floatPopMinimized;local targetSize=(floatPopMinimized and UDim2.new(0,220,0,32)) or UDim2.new(0,220,0,80) ;TweenService:Create(floatPop,TweenInfo.new(0.25,Enum.EasingStyle.Quad),{Size=targetSize}):Play();floatPopText.Visible= not floatPopMinimized;saveSettings(true);end);stealStatusPop=Instance.new("Frame");stealStatusPop.Size=UDim2.new(0,200,0,90);stealStatusPop.Position=UDim2.new(0,850,0,100);stealStatusPop.BackgroundColor3=PURPLE_DARK;stealStatusPop.BackgroundTransparency=0.2;stealStatusPop.Visible=false;stealStatusPop.Parent=screenGui;makeDraggable(stealStatusPop);Instance.new("UICorner",stealStatusPop).CornerRadius=UDim.new(0,16);stealStatusStroke=Instance.new("UIStroke",stealStatusPop);stealStatusStroke.Color=PURPLE_GLOW;stealStatusStroke.Thickness=3;stealStatusStroke.Transparency=0.3;stealStatusTitle=Instance.new("TextLabel");stealStatusTitle.Size=UDim2.new(1, -40,0,28);stealStatusTitle.Position=UDim2.new(0,8,0,4);stealStatusTitle.BackgroundTransparency=1;stealStatusTitle.Text="Auto Steal";stealStatusTitle.TextColor3=TEXT_COLOR;stealStatusTitle.Font=Enum.Font.GothamBold;stealStatusTitle.TextSize=18;stealStatusTitle.Parent=stealStatusPop;local progressTrack=Instance.new("Frame",stealStatusPop);progressTrack.Size=UDim2.new(1, -16,0,8);progressTrack.Position=UDim2.new(0,8,0,35);progressTrack.BackgroundColor3=PURPLE_MED;progressTrack.BackgroundTransparency=0.3;progressTrack.BorderSizePixel=0;Instance.new("UICorner",progressTrack).CornerRadius=UDim.new(1,0);progressBarFill=Instance.new("Frame",progressTrack);progressBarFill.Size=UDim2.new(0,0,1,0);progressBarFill.BackgroundColor3=PURPLE_BRIGHT;progressBarFill.BorderSizePixel=0;Instance.new("UICorner",progressBarFill).CornerRadius=UDim.new(1,0);stealStatusText=Instance.new("TextLabel",stealStatusPop);stealStatusText.Size=UDim2.new(1, -16,0,32);stealStatusText.Position=UDim2.new(0,8,0,50);stealStatusText.BackgroundColor3=PURPLE_MED;stealStatusText.BackgroundTransparency=0.3;stealStatusText.Text="OFF";stealStatusText.TextColor3=TEXT_COLOR;stealStatusText.Font=Enum.Font.GothamSemibold;stealStatusText.TextSize=16;Instance.new("UICorner",stealStatusText).CornerRadius=UDim.new(0,8);local stealStatusMinimizeBtn=Instance.new("TextButton",stealStatusPop);stealStatusMinimizeBtn.Size=UDim2.new(0,28,0,28);stealStatusMinimizeBtn.Position=UDim2.new(1, -32,0,4);stealStatusMinimizeBtn.BackgroundColor3=PURPLE_MED;stealStatusMinimizeBtn.BackgroundTransparency=0.3;stealStatusMinimizeBtn.Text="−";stealStatusMinimizeBtn.TextColor3=Color3.fromRGB(255,255,255);stealStatusMinimizeBtn.Font=Enum.Font.GothamBold;stealStatusMinimizeBtn.TextSize=26;Instance.new("UICorner",stealStatusMinimizeBtn).CornerRadius=UDim.new(0,10);local minimizeStroke=Instance.new("UIStroke",stealStatusMinimizeBtn);minimizeStroke.Color=PURPLE_GLOW;minimizeStroke.Thickness=2;local stealStatusMinimized=settings.StealStatusMinimized;stealStatusMinimizeBtn.MouseButton1Click:Connect(function() stealStatusMinimized= not stealStatusMinimized;settings.StealStatusMinimized=stealStatusMinimized;local targetSize=(stealStatusMinimized and UDim2.new(0,200,0,32)) or UDim2.new(0,200,0,90) ;TweenService:Create(stealStatusPop,TweenInfo.new(0.25,Enum.EasingStyle.Quad),{Size=targetSize}):Play();progressTrack.Visible= not stealStatusMinimized;stealStatusText.Visible= not stealStatusMinimized;saveSettings(true);end);local autoStealBtn=createToggleButton(pageMove,(autoStealEnabled and "Auto Steal: ON") or "Auto Steal: OFF" ,autoStealEnabled);autoStealBtn.MouseButton1Click:Connect(function() autoStealEnabled= not autoStealEnabled;settings.AutoSteal=autoStealEnabled;autoStealBtn.Text=(autoStealEnabled and "Auto Steal: ON") or "Auto Steal: OFF" ;autoStealBtn.BackgroundColor3=(autoStealEnabled and ON_COLOR) or OFF_COLOR ;autoStealBtn.BackgroundTransparency=(autoStealEnabled and 0.1) or 0.3 ;if autoStealEnabled then startAutoSteal();else stopAutoSteal();end saveSettings(true);end);local showSpeedBtn=createToggleButton(pageMove,"Speed Pop-Out",false);local speedPop=Instance.new("Frame");speedPop.Size=UDim2.new(0,200,0,110);speedPop.Position=UDim2.new(0,400,0,100);addPopoutStyle(speedPop);speedPop.Visible=settings.SpeedPopVisible;speedPop.Parent=screenGui;makeDraggable(speedPop);local speedPopTitle=Instance.new("TextLabel");speedPopTitle.Size=UDim2.new(1,0,0,32);speedPopTitle.BackgroundTransparency=1;speedPopTitle.Text="Speed Booster";speedPopTitle.TextColor3=TEXT_COLOR;speedPopTitle.Font=Enum.Font.GothamBold;speedPopTitle.TextSize=18;speedPopTitle.Parent=speedPop;local speedPopBox=Instance.new("TextBox");speedPopBox.Size=UDim2.new(1, -16,0,32);speedPopBox.Position=UDim2.new(0,8,0,35);speedPopBox.PlaceholderText="Speed Value";speedPopBox.Text=settings.SpeedText;speedPopBox.BackgroundColor3=PURPLE_MED;speedPopBox.BackgroundTransparency=0.3;speedPopBox.TextColor3=TEXT_COLOR;speedPopBox.Font=Enum.Font.GothamSemibold;speedPopBox.TextSize=16;speedPopBox.Parent=speedPop;Instance.new("UICorner",speedPopBox).CornerRadius=UDim.new(0,8);speedPopBox.FocusLost:Connect(function() settings.SpeedText=speedPopBox.Text;saveSettings(true);end);local toggleSpeedBtn=createToggleButton(speedPop,(settings.SpeedActive and "Booster: ON") or "Booster: OFF" ,settings.SpeedActive);toggleSpeedBtn.Size=UDim2.new(1, -16,0,32);toggleSpeedBtn.Position=UDim2.new(0,8,0,70);toggleSpeedBtn.TextSize=16;local speedActive=settings.SpeedActive;toggleSpeedBtn.MouseButton1Click:Connect(function() speedActive= not speedActive;settings.SpeedActive=speedActive;toggleSpeedBtn.Text=(speedActive and "Booster: ON") or "Booster: OFF" ;toggleSpeedBtn.BackgroundColor3=(speedActive and ON_COLOR) or OFF_COLOR ;toggleSpeedBtn.BackgroundTransparency=(speedActive and 0.1) or 0.3 ;saveSettings(true);end);local speedMinimize=Instance.new("TextButton");speedMinimize.Size=UDim2.new(0,28,0,28);speedMinimize.Position=UDim2.new(1, -32,0,4);speedMinimize.Parent=speedPop;addMinimizeStyle(speedMinimize);local speedMinimized=settings.SpeedMinimized;speedMinimize.MouseButton1Click:Connect(function() speedMinimized= not speedMinimized;settings.SpeedMinimized=speedMinimized;local targetSize=(speedMinimized and UDim2.new(0,200,0,32)) or UDim2.new(0,200,0,110) ;TweenService:Create(speedPop,TweenInfo.new(0.25,Enum.EasingStyle.Quad),{Size=targetSize}):Play();speedPopBox.Visible= not speedMinimized;toggleSpeedBtn.Visible= not speedMinimized;saveSettings(true);end);showSpeedBtn.MouseButton1Click:Connect(function() speedPop.Visible= not speedPop.Visible;settings.SpeedPopVisible=speedPop.Visible;saveSettings(true);end);local fpsPingPop=Instance.new("Frame");fpsPingPop.Size=UDim2.new(0,220,0,150);fpsPingPop.Position=UDim2.new(0,800,0,100);addPopoutStyle(fpsPingPop);fpsPingPop.Visible=settings.FPSPingPopVisible;fpsPingPop.Parent=screenGui;makeDraggable(fpsPingPop);local fpsPingTitle=Instance.new("TextLabel");fpsPingTitle.Size=UDim2.new(1,0,0,32);fpsPingTitle.BackgroundTransparency=1;fpsPingTitle.Text="FPS & Ping";fpsPingTitle.TextColor3=TEXT_COLOR;fpsPingTitle.Font=Enum.Font.GothamBold;fpsPingTitle.TextSize=18;fpsPingTitle.Parent=fpsPingPop;local clientFPSLabel=Instance.new("TextLabel");clientFPSLabel.Size=UDim2.new(1, -16,0,32);clientFPSLabel.Position=UDim2.new(0,8,0,35);clientFPSLabel.BackgroundTransparency=1;clientFPSLabel.Text="Client FPS: Calculating...";clientFPSLabel.TextColor3=Color3.fromRGB(0,255,100);clientFPSLabel.Font=Enum.Font.GothamBold;clientFPSLabel.TextSize=22;clientFPSLabel.TextXAlignment=Enum.TextXAlignment.Left;clientFPSLabel.Parent=fpsPingPop;local serverFPSLabel=Instance.new("TextLabel");serverFPSLabel.Size=UDim2.new(1, -16,0,32);serverFPSLabel.Position=UDim2.new(0,8,0,68);serverFPSLabel.BackgroundTransparency=1;serverFPSLabel.Text="Server FPS: Calculating...";serverFPSLabel.TextColor3=Color3.fromRGB(0,200,255);serverFPSLabel.Font=Enum.Font.GothamBold;serverFPSLabel.TextSize=22;serverFPSLabel.TextXAlignment=Enum.TextXAlignment.Left;serverFPSLabel.Parent=fpsPingPop;local pingLabel=Instance.new("TextLabel");pingLabel.Size=UDim2.new(1, -16,0,32);pingLabel.Position=UDim2.new(0,8,0,101);pingLabel.BackgroundTransparency=1;pingLabel.Text="Ping: Calculating...";pingLabel.TextColor3=Color3.fromRGB(255,255,0);pingLabel.Font=Enum.Font.GothamBold;pingLabel.TextSize=22;pingLabel.TextXAlignment=Enum.TextXAlignment.Left;pingLabel.Parent=fpsPingPop;local fpsPingMinimize=Instance.new("TextButton");fpsPingMinimize.Size=UDim2.new(0,28,0,28);fpsPingMinimize.Position=UDim2.new(1, -32,0,4);fpsPingMinimize.Parent=fpsPingPop;addMinimizeStyle(fpsPingMinimize);local fpsPingMinimized=settings.FPSPingMinimized;fpsPingMinimize.MouseButton1Click:Connect(function() fpsPingMinimized= not fpsPingMinimized;settings.FPSPingMinimized=fpsPingMinimized;local targetSize=(fpsPingMinimized and UDim2.new(0,220,0,32)) or UDim2.new(0,220,0,150) ;TweenService:Create(fpsPingPop,TweenInfo.new(0.25,Enum.EasingStyle.Quad),{Size=targetSize}):Play();clientFPSLabel.Visible= not fpsPingMinimized;serverFPSLabel.Visible= not fpsPingMinimized;pingLabel.Visible= not fpsPingMinimized;saveSettings(true);end);local fpsPingToggleBtn=createToggleButton(pageMisc,"FPS & Ping Pop-Out",false);fpsPingToggleBtn.MouseButton1Click:Connect(function() fpsPingPop.Visible= not fpsPingPop.Visible;settings.FPSPingPopVisible=fpsPingPop.Visible;saveSettings(true);end);local optimizerBtn=createToggleButton(pageMisc,(settings.Optimizer and "Optimizer: ON") or "Optimizer: OFF" ,settings.Optimizer);local optimizerEnabled=settings.Optimizer;local optimizerOriginals={Lighting={},Terrain={},Objects={}};local function applyOptimizer() local Lighting=game:GetService("Lighting");local Terrain=workspace:FindFirstChildOfClass("Terrain");optimizerOriginals.Lighting.GlobalShadows=Lighting.GlobalShadows;optimizerOriginals.Lighting.Brightness=Lighting.Brightness;optimizerOriginals.Lighting.FogEnd=Lighting.FogEnd;Lighting.GlobalShadows=false;Lighting.Brightness=1;Lighting.FogEnd=1000000000;for _,effect in pairs(Lighting:GetChildren()) do if effect:IsA("PostEffect") then optimizerOriginals.Lighting[effect]=effect.Enabled;effect.Enabled=false;end end if Terrain then optimizerOriginals.Terrain.WaterWaveSize=Terrain.WaterWaveSize;optimizerOriginals.Terrain.WaterWaveSpeed=Terrain.WaterWaveSpeed;optimizerOriginals.Terrain.WaterReflectance=Terrain.WaterReflectance;optimizerOriginals.Terrain.WaterTransparency=Terrain.WaterTransparency;Terrain.WaterWaveSize=0;Terrain.WaterWaveSpeed=0;Terrain.WaterReflectance=0;Terrain.WaterTransparency=1;end for _,obj in pairs(workspace:GetDescendants()) do if obj:IsA("BasePart") then optimizerOriginals.Objects[obj]={Material=obj.Material,Reflectance=obj.Reflectance};obj.Material=Enum.Material.Plastic;obj.Reflectance=0;elseif (obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire")) then optimizerOriginals.Objects[obj]={Enabled=obj.Enabled};obj.Enabled=false;end end pcall(function() settings().Rendering.QualityLevel=Enum.QualityLevel.Level01;settings().Rendering.MeshPartDetailLevel=Enum.MeshPartDetailLevel.Level01;end);end local function restoreOptimizer() local Lighting=game:GetService("Lighting");local Terrain=workspace:FindFirstChildOfClass("Terrain");if (optimizerOriginals.Lighting.GlobalShadows~=nil) then Lighting.GlobalShadows=optimizerOriginals.Lighting.GlobalShadows;end if (optimizerOriginals.Lighting.Brightness~=nil) then Lighting.Brightness=optimizerOriginals.Lighting.Brightness;end if (optimizerOriginals.Lighting.FogEnd~=nil) then Lighting.FogEnd=optimizerOriginals.Lighting.FogEnd;end for effect,enabled in pairs(optimizerOriginals.Lighting) do if ((typeof(effect)=="Instance") and effect:IsA("PostEffect")) then effect.Enabled=enabled;end end if (Terrain and (optimizerOriginals.Terrain.WaterWaveSize~=nil)) then Terrain.WaterWaveSize=optimizerOriginals.Terrain.WaterWaveSize;Terrain.WaterWaveSpeed=optimizerOriginals.Terrain.WaterWaveSpeed;Terrain.WaterReflectance=optimizerOriginals.Terrain.WaterReflectance;Terrain.WaterTransparency=optimizerOriginals.Terrain.WaterTransparency;end for obj,props in pairs(optimizerOriginals.Objects) do if (obj and obj.Parent) then if obj:IsA("BasePart") then obj.Material=props.Material;obj.Reflectance=props.Reflectance;elseif (obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire")) then obj.Enabled=props.Enabled;end end end optimizerOriginals={Lighting={},Terrain={},Objects={}};end optimizerBtn.MouseButton1Click:Connect(function() optimizerEnabled= not optimizerEnabled;settings.Optimizer=optimizerEnabled;optimizerBtn.Text=(optimizerEnabled and "Optimizer: ON") or "Optimizer: OFF" ;optimizerBtn.BackgroundColor3=(optimizerEnabled and ON_COLOR) or OFF_COLOR ;optimizerBtn.BackgroundTransparency=(optimizerEnabled and 0.1) or 0.3 ;if optimizerEnabled then applyOptimizer();else restoreOptimizer();end saveSettings(true);end);local discordBtn=createToggleButton(pageMisc,"Copy Discord Server",false);discordBtn.BackgroundColor3=Color3.fromRGB(88,101,242);discordBtn.BackgroundTransparency=0.3;discordBtn.MouseButton1Click:Connect(function() setclipboard("https://discord.gg/rQzYhhRdHk                 ");StarterGui:SetCore("SendNotification",{Title="RockHub",Text="Discord link copied!",Duration=3});end);local kickMeBtn=createToggleButton(pageMisc,"Kick Me",false);kickMeBtn.BackgroundColor3=Color3.fromRGB(180,30,30);kickMeBtn.BackgroundTransparency=0.3;kickMeBtn.MouseButton1Click:Connect(function() LocalPlayer:Kick("Kicked by RockHub");end);local bindables={};local function createBindRow(labelText,bindable) local row=Instance.new("Frame");row.Size=UDim2.new(1,0,0,45);row.BackgroundTransparency=1;row.Parent=pageKeybinds;local lbl=Instance.new("TextLabel");lbl.Size=UDim2.new(0.5,0,1,0);lbl.Position=UDim2.new(0,12,0,0);lbl.BackgroundTransparency=1;lbl.Text=labelText;lbl.TextColor3=TEXT_COLOR;lbl.Font=Enum.Font.GothamSemibold;lbl.TextSize=17;lbl.TextXAlignment=Enum.TextXAlignment.Left;lbl.Parent=row;local btn=Instance.new("TextButton");btn.Size=UDim2.new(0.45, -12,0,36);btn.Position=UDim2.new(0.55,0,0,4);btn.BackgroundColor3=PURPLE_MED;btn.BackgroundTransparency=0.3;btn.TextColor3=TEXT_COLOR;btn.Font=Enum.Font.GothamBold;btn.TextSize=17;btn.Parent=row;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,10);local stroke=Instance.new("UIStroke");stroke.Color=PURPLE_GLOW;stroke.Thickness=1.5;stroke.Transparency=0.5;stroke.Parent=btn;bindable.button=btn;local display=(bindable.key and bindable.key.Name) or "None" ;btn.Text="["   .. display   .. "]" ;btn.MouseButton1Click:Connect(function() btn.Text="Press key... (Backspace = None)";currentWaiting=bindable;end);end table.insert(bindables,{name="Menu Toggle",key=settings.GUIKey,button=nil,action=function() mainFrame.Visible= not mainFrame.Visible;end,settingKey="GUIKey"});table.insert(bindables,{name="ESP",key=settings.ESPKey,button=nil,action=function() espEnabled= not espEnabled;settings.ESP=espEnabled;espBtn.Text=(espEnabled and "ESP: ON") or "ESP: OFF" ;espBtn.BackgroundColor3=(espEnabled and ON_COLOR) or OFF_COLOR ;espBtn.BackgroundTransparency=(espEnabled and 0.1) or 0.3 ;end,settingKey="ESPKey"});table.insert(bindables,{name="X-Ray",key=settings.XRayKey,button=nil,action=function() xrayEnabled= not xrayEnabled;settings.XRay=xrayEnabled;xrayBtn.Text=(xrayEnabled and "X-Ray: ON") or "X-Ray: OFF" ;xrayBtn.BackgroundColor3=(xrayEnabled and ON_COLOR) or OFF_COLOR ;xrayBtn.BackgroundTransparency=(xrayEnabled and 0.1) or 0.3 ;if xrayEnabled then enableXRay();else clearXRay();end end,settingKey="XRayKey"});table.insert(bindables,{name="Hitbox Expander",key=settings.HitboxExpanderKey,button=nil,action=function() hitboxExpanderEnabled= not hitboxExpanderEnabled;settings.HitboxExpander=hitboxExpanderEnabled;hitboxExpanderBtn.Text=(hitboxExpanderEnabled and "Hitbox Expander: ON") or "Hitbox Expander: OFF" ;hitboxExpanderBtn.BackgroundColor3=(hitboxExpanderEnabled and ON_COLOR) or OFF_COLOR ;hitboxExpanderBtn.BackgroundTransparency=(hitboxExpanderEnabled and 0.1) or 0.3 ;if hitboxExpanderEnabled then enableHitboxExpander();else disableHitboxExpander();end saveSettings(true);end,settingKey="HitboxExpanderKey"});table.insert(bindables,{name="Low Gravity",key=settings.GravityKey,button=nil,action=function() gravityOn= not gravityOn;settings.Gravity=gravityOn;gravityBtn.Text=(gravityOn and "Low Gravity: ON") or "Low Gravity: OFF" ;gravityBtn.BackgroundColor3=(gravityOn and ON_COLOR) or OFF_COLOR ;gravityBtn.BackgroundTransparency=(gravityOn and 0.1) or 0.3 ;local char=player.Character;local hrp=char and char:FindFirstChild("HumanoidRootPart") ;if (gravityOn and hrp) then if force then force:Destroy();end force=Instance.new("BodyForce");force.Force=Vector3.new(0,workspace.Gravity * hrp:GetMass() * 0.7 ,0);force.Parent=hrp;elseif force then force:Destroy();force=nil;end end,settingKey="GravityKey"});table.insert(bindables,{name="No Walk Anim",key=settings.UnwalkKey,button=nil,action=function() unwalk= not unwalk;settings.Unwalk=unwalk;unwalkBtn.Text=(unwalk and "No Walk Anim: ON") or "No Walk Anim: OFF" ;unwalkBtn.BackgroundColor3=(unwalk and ON_COLOR) or OFF_COLOR ;unwalkBtn.BackgroundTransparency=(unwalk and 0.1) or 0.3 ;updateUnwalk();end,settingKey="UnwalkKey"});table.insert(bindables,{name="Anti Knockback",key=settings.AntiKnockbackKey,button=nil,action=function() settings.AntiKnockback= not settings.AntiKnockback;antiKBBtn.Text=(settings.AntiKnockback and "Anti Knockback: ON") or "Anti Knockback: OFF" ;antiKBBtn.BackgroundColor3=(settings.AntiKnockback and ON_COLOR) or OFF_COLOR ;antiKBBtn.BackgroundTransparency=(settings.AntiKnockback and 0.1) or 0.3 ;if settings.AntiKnockback then EnableAntiKB();else DisableAntiKB();end end,settingKey="AntiKnockbackKey"});table.insert(bindables,{name="Speed 55",key=settings.Speed55Key,button=nil,action=function() speed55Active= not speed55Active;settings.Speed55=speed55Active;speed55Btn.Text=(speed55Active and "Speed 55: ON") or "Speed 55: OFF" ;speed55Btn.BackgroundColor3=(speed55Active and ON_COLOR) or OFF_COLOR ;speed55Btn.BackgroundTransparency=(speed55Active and 0.1) or 0.3 ;end,settingKey="Speed55Key"});table.insert(bindables,{name="Speed Booster",key=settings.SpeedBoosterKey,button=nil,action=function() speedActive= not speedActive;settings.SpeedActive=speedActive;toggleSpeedBtn.Text=(speedActive and "Booster: ON") or "Booster: OFF" ;toggleSpeedBtn.BackgroundColor3=(speedActive and ON_COLOR) or OFF_COLOR ;toggleSpeedBtn.BackgroundTransparency=(speedActive and 0.1) or 0.3 ;end,settingKey="SpeedBoosterKey"});table.insert(bindables,{name="Optimizer",key=settings.OptimizerKey,button=nil,action=function() optimizerEnabled= not optimizerEnabled;settings.Optimizer=optimizerEnabled;optimizerBtn.Text=(optimizerEnabled and "Optimizer: ON") or "Optimizer: OFF" ;optimizerBtn.BackgroundColor3=(optimizerEnabled and ON_COLOR) or OFF_COLOR ;optimizerBtn.BackgroundTransparency=(optimizerEnabled and 0.1) or 0.3 ;if optimizerEnabled then applyOptimizer();else restoreOptimizer();end end,settingKey="OptimizerKey"});table.insert(bindables,{name="Spin",key=settings.SpinKey,button=nil,action=function() spinEnabled= not spinEnabled;settings.Spin=spinEnabled;spinBtn.Text=(spinEnabled and "Spin: ON") or "Spin: OFF" ;spinBtn.BackgroundColor3=(spinEnabled and ON_COLOR) or OFF_COLOR ;spinBtn.BackgroundTransparency=(spinEnabled and 0.1) or 0.3 ;if (spinEnabled and player.Character) then applySpin(player.Character);else removeSpin();end end,settingKey="SpinKey"});table.insert(bindables,{name="Float",key=settings.FloatKey,button=nil,action=function() floatEnabled= not floatEnabled;settings.Float=floatEnabled;floatBtn.Text=(floatEnabled and "Float: ON") or "Float: OFF" ;floatBtn.BackgroundColor3=(floatEnabled and ON_COLOR) or OFF_COLOR ;floatBtn.BackgroundTransparency=(floatEnabled and 0.1) or 0.3 ;local hrp=player.Character and player.Character:FindFirstChild("HumanoidRootPart") ;if (floatEnabled and hrp) then if  not targetFloatY then targetFloatY=hrp.Position.Y + FLOAT_HEIGHT ;end createFloatPlatform();setupFloatBP(hrp);startFloatCountdown();else if floatBP then floatBP:Destroy();floatBP=nil;end if floatPart then floatPart:Destroy();floatPart=nil;end targetFloatY=nil;stopFloatCountdown();end saveSettings(true);end,settingKey="FloatKey"});table.insert(bindables,{name="Auto Bat",key=settings.AutoBatKey,button=nil,action=function() autoBatEnabled= not autoBatEnabled;settings.AutoBat=autoBatEnabled;autoBatBtn.Text=(autoBatEnabled and "Auto Bat: ON") or "Auto Bat: OFF" ;autoBatBtn.BackgroundColor3=(autoBatEnabled and ON_COLOR) or OFF_COLOR ;autoBatBtn.BackgroundTransparency=(autoBatEnabled and 0.1) or 0.3 ;if autoBatEnabled then equipBat();createBatRadiusVisual();else cleanupBatRadius();end saveSettings(true);end,settingKey="AutoBatKey"});table.insert(bindables,{name="Auto Steal",key=settings.AutoStealKey,button=nil,action=function() autoStealEnabled= not autoStealEnabled;settings.AutoSteal=autoStealEnabled;autoStealBtn.Text=(autoStealEnabled and "Auto Steal: ON") or "Auto Steal: OFF" ;autoStealBtn.BackgroundColor3=(autoStealEnabled and ON_COLOR) or OFF_COLOR ;autoStealBtn.BackgroundTransparency=(autoStealEnabled and 0.1) or 0.3 ;if autoStealEnabled then startAutoSteal();else stopAutoSteal();end saveSettings(true);end,settingKey="AutoStealKey"});for _,b in ipairs(bindables) do if (b.name~="Teleport") then createBindRow(b.name,b);end end inputConnection=UserInputService.InputBegan:Connect(function(input,gpe) if gpe then return;end if (currentWaiting and (input.UserInputType==Enum.UserInputType.Keyboard)) then if ((input.KeyCode==Enum.KeyCode.Backspace) or (input.KeyCode==Enum.KeyCode.Delete)) then currentWaiting.key=nil;settings[currentWaiting.settingKey]=nil;local display="None";if currentWaiting.button then currentWaiting.button.Text="["   .. display   .. "]" ;end saveSettings(true);currentWaiting=nil;elseif (input.KeyCode~=Enum.KeyCode.Unknown) then currentWaiting.key=input.KeyCode;settings[currentWaiting.settingKey]=input.KeyCode;local display=input.KeyCode.Name;if currentWaiting.button then currentWaiting.button.Text="["   .. display   .. "]" ;end saveSettings(true);currentWaiting=nil;end elseif (input.UserInputType==Enum.UserInputType.Keyboard) then for _,b in ipairs(bindables) do if (b.key and (input.KeyCode==b.key)) then b.action();break;end end end end);local function updateUIFromSettings() espEnabled=settings.ESP;espBtn.Text=(espEnabled and "ESP: ON") or "ESP: OFF" ;espBtn.BackgroundColor3=(espEnabled and ON_COLOR) or OFF_COLOR ;espBtn.BackgroundTransparency=(espEnabled and 0.1) or 0.3 ;espColor=settings.ESPColor;gravityOn=settings.Gravity;gravityBtn.Text=(gravityOn and "Low Gravity: ON") or "Low Gravity: OFF" ;gravityBtn.BackgroundColor3=(gravityOn and ON_COLOR) or OFF_COLOR ;gravityBtn.BackgroundTransparency=(gravityOn and 0.1) or 0.3 ;unwalk=settings.Unwalk;unwalkBtn.Text=(unwalk and "No Walk Anim: ON") or "No Walk Anim: OFF" ;unwalkBtn.BackgroundColor3=(unwalk and ON_COLOR) or OFF_COLOR ;unwalkBtn.BackgroundTransparency=(unwalk and 0.1) or 0.3 ;updateUnwalk();if settings.AntiKnockback then EnableAntiKB();else DisableAntiKB();end antiKBBtn.Text=(settings.AntiKnockback and "Anti Knockback: ON") or "Anti Knockback: OFF" ;antiKBBtn.BackgroundColor3=(settings.AntiKnockback and ON_COLOR) or OFF_COLOR ;antiKBBtn.BackgroundTransparency=(settings.AntiKnockback and 0.1) or 0.3 ;speed55Active=settings.Speed55;speed55Btn.Text=(speed55Active and "Speed 55: ON") or "Speed 55: OFF" ;speed55Btn.BackgroundColor3=(speed55Active and ON_COLOR) or OFF_COLOR ;speed55Btn.BackgroundTransparency=(speed55Active and 0.1) or 0.3 ;spinEnabled=settings.Spin;spinBtn.Text=(spinEnabled and "Spin: ON") or "Spin: OFF" ;spinBtn.BackgroundColor3=(spinEnabled and ON_COLOR) or OFF_COLOR ;spinBtn.BackgroundTransparency=(spinEnabled and 0.1) or 0.3 ;if (spinEnabled and player.Character) then applySpin(player.Character);else removeSpin();end floatEnabled=settings.Float;floatBtn.Text=(floatEnabled and "Float: ON") or "Float: OFF" ;floatBtn.BackgroundColor3=(floatEnabled and ON_COLOR) or OFF_COLOR ;floatBtn.BackgroundTransparency=(floatEnabled and 0.1) or 0.3 ;if floatEnabled then local hrp=player.Character and player.Character:FindFirstChild("HumanoidRootPart") ;if hrp then if  not targetFloatY then targetFloatY=hrp.Position.Y + FLOAT_HEIGHT ;end createFloatPlatform();setupFloatBP(hrp);startFloatCountdown();end else if floatBP then floatBP:Destroy();floatBP=nil;end if floatPart then floatPart:Destroy();floatPart=nil;end targetFloatY=nil;stopFloatCountdown();end autoBatEnabled=settings.AutoBat;autoBatBtn.Text=(autoBatEnabled and "Auto Bat: ON") or "Auto Bat: OFF" ;autoBatBtn.BackgroundColor3=(autoBatEnabled and ON_COLOR) or OFF_COLOR ;autoBatBtn.BackgroundTransparency=(autoBatEnabled and 0.1) or 0.3 ;if autoBatEnabled then createBatRadiusVisual();end autoStealEnabled=settings.AutoSteal;autoStealBtn.Text=(autoStealEnabled and "Auto Steal: ON") or "Auto Steal: OFF" ;autoStealBtn.BackgroundColor3=(autoStealEnabled and ON_COLOR) or OFF_COLOR ;autoStealBtn.BackgroundTransparency=(autoStealEnabled and 0.1) or 0.3 ;if autoStealEnabled then startAutoSteal();end if stealStatusPop then stealStatusMinimized=settings.StealStatusMinimized;stealStatusPop.Size=(stealStatusMinimized and UDim2.new(0,200,0,32)) or UDim2.new(0,200,0,90) ;end floatPopMinimized=settings.FloatPopMinimized;if floatPop then floatPop.Size=(floatPopMinimized and UDim2.new(0,220,0,32)) or UDim2.new(0,220,0,80) ;floatPopText.Visible= not floatPopMinimized;end speedPop.Visible=settings.SpeedPopVisible;speedMinimized=settings.SpeedMinimized;speedActive=settings.SpeedActive;speedPopBox.Text=settings.SpeedText;toggleSpeedBtn.Text=(speedActive and "Booster: ON") or "Booster: OFF" ;toggleSpeedBtn.BackgroundColor3=(speedActive and ON_COLOR) or OFF_COLOR ;toggleSpeedBtn.BackgroundTransparency=(speedActive and 0.1) or 0.3 ;speedPop.Size=(speedMinimized and UDim2.new(0,200,0,32)) or UDim2.new(0,200,0,110) ;speedPopBox.Visible= not speedMinimized;toggleSpeedBtn.Visible= not speedMinimized;fpsPingPop.Visible=settings.FPSPingPopVisible;fpsPingMinimized=settings.FPSPingMinimized;fpsPingPop.Size=(fpsPingMinimized and UDim2.new(0,220,0,32)) or UDim2.new(0,220,0,150) ;clientFPSLabel.Visible= not fpsPingMinimized;serverFPSLabel.Visible= not fpsPingMinimized;pingLabel.Visible= not fpsPingMinimized;xrayEnabled=settings.XRay;xrayBtn.Text=(xrayEnabled and "X-Ray: ON") or "X-Ray: OFF" ;xrayBtn.BackgroundColor3=(xrayEnabled and ON_COLOR) or OFF_COLOR ;xrayBtn.BackgroundTransparency=(xrayEnabled and 0.1) or 0.3 ;if xrayEnabled then enableXRay();else clearXRay();end optimizerEnabled=settings.Optimizer;optimizerBtn.Text=(optimizerEnabled and "Optimizer: ON") or "Optimizer: OFF" ;optimizerBtn.BackgroundColor3=(optimizerEnabled and ON_COLOR) or OFF_COLOR ;optimizerBtn.BackgroundTransparency=(optimizerEnabled and 0.1) or 0.3 ;if optimizerEnabled then applyOptimizer();else restoreOptimizer();end hitboxExpanderEnabled=settings.HitboxExpander;hitboxExpanderBtn.Text=(hitboxExpanderEnabled and "Hitbox Expander: ON") or "Hitbox Expander: OFF" ;hitboxExpanderBtn.BackgroundColor3=(hitboxExpanderEnabled and ON_COLOR) or OFF_COLOR ;hitboxExpanderBtn.BackgroundTransparency=(hitboxExpanderEnabled and 0.1) or 0.3 ;if hitboxExpanderEnabled then enableHitboxExpander();else disableHitboxExpander();end end updateUIFromSettings();local clientFrameCount=0;local physicsFrameCount=0;local lastUpdateTime=tick();local function getPing() local pingItem=Stats.Network and Stats.Network.ServerStatsItem and Stats.Network.ServerStatsItem:FindFirstChild("Data Ping") ;if pingItem then return math.round(pingItem:GetValue());end return "N/A";end physicsConnection=RunService.Stepped:Connect(function() physicsFrameCount=physicsFrameCount + 1 ;end);heartbeatConnection=RunService.Heartbeat:Connect(function() if uiClosed then return;end clientFrameCount=clientFrameCount + 1 ;local currentTime=tick();if ((currentTime-lastUpdateTime)>=1) then local elapsed=currentTime-lastUpdateTime ;local clientFPS=((elapsed>0) and math.floor((clientFrameCount/elapsed) + 0.5 )) or 0 ;local serverFPS=((elapsed>0) and math.floor((physicsFrameCount/elapsed) + 0.5 )) or 0 ;local ping=getPing();if (fpsPingPop.Visible and  not fpsPingMinimized) then clientFPSLabel.Text="Client FPS: "   .. clientFPS ;serverFPSLabel.Text="Server FPS: "   .. serverFPS ;pingLabel.Text="Ping: "   .. ping   .. (((ping~="N/A") and " ms") or "") ;end clientFrameCount=0;physicsFrameCount=0;lastUpdateTime=currentTime;end if espEnabled then for _,plr in ipairs(Players:GetPlayers()) do if (plr==LocalPlayer) then continue;end local char=plr.Character;local hrp=char and char:FindFirstChild("HumanoidRootPart") ;if  not espObjects[plr] then local box=Drawing.new("Square");box.Visible=false;box.Color=espColor;box.Thickness=2;box.Filled=true;box.Transparency=0.4;local name=Drawing.new("Text");name.Visible=false;name.Color=espColor;name.Size=20;name.Center=true;name.Outline=true;name.Font=2;espObjects[plr]={box=box,name=name};plr.CharacterRemoving:Connect(function() if espObjects[plr] then espObjects[plr].box:Remove();espObjects[plr].name:Remove();espObjects[plr]=nil;end end);end local data=espObjects[plr];if hrp then local top,inViewport=workspace.CurrentCamera:WorldToViewportPoint(hrp.CFrame.Position + Vector3.new(0,3.5,0) );local bottom=workspace.CurrentCamera:WorldToViewportPoint(hrp.CFrame.Position-Vector3.new(0,3.5,0) );local height=math.abs(top.Y-bottom.Y );local width=height * 0.6 ;data.box.Visible=inViewport;data.box.Color=espColor;data.box.Size=Vector2.new(width,height);data.box.Position=Vector2.new(top.X-(width/2) ,top.Y);data.name.Visible=inViewport;data.name.Color=espColor;data.name.Text=plr.Name;data.name.Position=Vector2.new(top.X,top.Y-22 );else data.box.Visible=false;data.name.Visible=false;end end else for _,v in pairs(espObjects) do v.box.Visible=false;v.name.Visible=false;end end local character=player.Character;local rootPart=character and character:FindFirstChild("HumanoidRootPart") ;local humanoid=character and character:FindFirstChildOfClass("Humanoid") ;if ((speedActive or speed55Active) and rootPart and humanoid and (humanoid.MoveDirection.Magnitude>0)) then local targetSpeed=(speed55Active and 55) or tonumber(speedPopBox.Text) or 16 ;local velocity=humanoid.MoveDirection * targetSpeed ;rootPart.AssemblyLinearVelocity=Vector3.new(velocity.X,rootPart.AssemblyLinearVelocity.Y,velocity.Z);end if (spinEnabled and spinBAV and spinBAV.Parent) then local shiftLockOn=UserInputService.MouseBehavior==Enum.MouseBehavior.LockCenter ;spinBAV.AngularVelocity=(shiftLockOn and Vector3.zero) or Vector3.new(0,SPIN_SPEED,0) ;end if (floatEnabled and floatBP and floatBP.Parent and rootPart and targetFloatY) then floatBP.Position=Vector3.new(rootPart.Position.X,targetFloatY,rootPart.Position.Z);end if (floatEnabled and floatPart and rootPart) then local offset=(rootPart.Size.Y/2) + (floatPart.Size.Y/2) + 0.2 ;floatPart.Position=Vector3.new(rootPart.Position.X,rootPart.Position.Y-offset ,rootPart.Position.Z);end updateBatRadiusVisual();if autoBatEnabled then if ( not currentBatTool or (currentBatTool.Parent~=character) or (currentBatTool.Name~="Bat")) then autoBatEnabled=false;settings.AutoBat=false;autoBatBtn.Text="Auto Bat: OFF";autoBatBtn.BackgroundColor3=OFF_COLOR;autoBatBtn.BackgroundTransparency=0.3;cleanupBatRadius();elseif isPlayerInRange(BAT_RANGE) then if ((currentTime-lastBatHit)>=BAT_HIT_DELAY) then lastBatHit=currentTime;pcall(function() currentBatTool:Activate();end);end end end end);closeBtn.MouseButton1Click:Connect(function() uiClosed=true;if heartbeatConnection then heartbeatConnection:Disconnect();end if physicsConnection then physicsConnection:Disconnect();end if inputConnection then inputConnection:Disconnect();end RunService:UnbindFromRenderStep("StopAnims");for _,v in pairs(espObjects) do v.box:Remove();v.name:Remove();end table.clear(espObjects);if force then force:Destroy();end removeSpin();if xrayEnabled then clearXRay();end if optimizerEnabled then restoreOptimizer();end DisableAntiKB();if floatBP then floatBP:Destroy();floatBP=nil;end if floatPart then floatPart:Destroy();floatPart=nil;end targetFloatY=nil;stopAutoSteal();stopFloatCountdown();cleanupBatRadius();disableHitboxExpander();screenGui:Destroy();end);if configLoaded then StarterGui:SetCore("SendNotification",{Title="RockHub",Text="Config loaded!",Duration=1.5});end end
